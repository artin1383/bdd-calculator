# گزارش کوتاه رفع خطای undefined

در Scenario Outline صفحهٔ ۱۸ مستند، ردیف `| -1 | 6 | 5 |` با مشکل undefined مواجه می‌شود؛ زیرا گام `Given Two input values, -1 and 6` با عبارت منظم `^Two input values, (\d+) and (\d+)$` تطبیق ندارد. الگوی `\d+` فقط رقم‌ها را می‌پذیرد و علامت منفی را پوشش نمی‌دهد. بنابراین علت خطا پیدا نشدن Step Definition است، نه اشتباه در محاسبهٔ جمع؛ مراحل بعدی این سناریو نیز اجرا نمی‌شوند.

برای رفع مشکل، الگوی ورودی به `^Two input values, (-?\d+) and (-?\d+)$` تغییر داده شد. علامت `-?` وجود اختیاری علامت منفی را مجاز می‌کند. همین اصلاح برای گام Then اعمال شد تا نتیجهٔ منفی نیز پذیرفته شود. در رشته‌های جاوا باید بک‌اسلش را دو بار نوشت: `@Given("^Two input values, (-?\\d+) and (-?\\d+)$")`. دو ردیف دیگر جدول اصلی از ابتدا قابل تطبیق هستند.

## قرارداد موارد مشخص‌نشده در صورت سؤال

ورودی و خروجی از نوع عدد صحیح ۳۲بیتی هستند؛ تقسیم غیرصحیح به سمت صفر قطع می‌شود. توان با ضرب محاسبه می‌شود، توان منفی خطاست و برای توان صفر، از جمله صفر به توان صفر، مقدار ۱ در نظر گرفته شده است. تقسیم بر صفر، عملگر نامعتبر و سرریز عدد صحیح نیز با خطای مشخص آزمون شده‌اند. این موارد تصمیم‌های این پیاده‌سازی هستند و در صورت سؤال به‌صراحت تعیین نشده‌اند.
